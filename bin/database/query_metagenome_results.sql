SELECT status, count(*)
FROM SEQUENCING_SUBSET_MACHINE
GROUP BY status;

SELECT *
FROM SEQUENCING_SUBSET_MACHINE t0
LEFT OUTER JOIN LOG_MACHINE_PROCESSING t1
ON t1.SEQUENCING_SUBSET_ID = t0.SEQUENCING_SUBSET_ID
WHERE status = 'FAILED'
ORDER BY t0.SEQUENCING_SUBSET_ID, t1.id DESC;

SELECT Number_of_unprocessed_sequence_subsets(2) FROM DUAL;

--DELETE FROM SEQUENCING_SUBSET_MACHINE
--WHERE STATUS = 'EMPTIED' or status = 'FAILED';

commit;